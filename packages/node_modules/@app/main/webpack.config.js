const path = require("path");

const OUTDIR = path.resolve(__dirname, 'dist');

module.exports = {
    entry: './src/index.ts',
    module: {
        rules: [
            {
                test: /\.tsx?$/,
                use: 'ts-loader',
                exclude: /node_modules/
            }
        ]
    },
    resolve: {
        extensions: ['.tsx', '.ts', '.js']
    },
    output: {
        filename: 'bundle.js',
        path: OUTDIR
    },
    optimization: {
        runtimeChunk: 'runtime'
    },
    devServer: {
        contentBase: OUTDIR,
        compress: true,
        port: 9000,
        allowedHosts: [
            '.local',
            '.local.twopitau.com'
        ],
        host: 'hmr.local.twopitau.com',
        hot: true,
    }
};
